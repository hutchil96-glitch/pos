{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile && node ./build.mjs"
  },
  "deploy": {
    "startCommand": "node ./dist/index.mjs"
  }
}
